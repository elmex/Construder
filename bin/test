#!/opt/perl/bin/perl
use Math::VectorReal;
$Math::VectorReal::FORMAT = "[ %9.4f, %9.4f, %9.4f ]";
use common::sense;
use Games::Blockminer3D::Client;
srand ($ARGV[0] || 10);

my $game = Games::Blockminer3D::Client->new;

my $scene = "XXX XXX\nXXX XXX\nX X X X\n";

$game->start;
